import Root from './button.svelte';
import { buttonVariants } from './button.variants';
import type { ButtonProps, ButtonSize, ButtonVariant } from './button.variants';

export {
  Root,
  type ButtonProps as Props,
  //
  Root as Button,
  buttonVariants,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
};
